---
title: Pull Build Deploy
description: Git pull then build and deploy the project
---

## Pull, build and deploy

<Tabs items={['FR', 'EN']}>
  <Tab value="FR">
    ```text title="Prompt FR"
    Effectue un git pull, puis construis et déploie le projet.
    ```
  </Tab>
  <Tab value="EN">
    ```text title="Prompt EN"
    Perform a git pull, then build and deploy the project.
    ```
  </Tab>
</Tabs>
