---
title: Optimize Code
description: Explore and optimize code at file, folder, or PRD level
---

## Folder

<Tabs items={['FR', 'EN']}>
  <Tab value="FR">
    ```text title="Prompt FR"
    Explore le code des fichiers du dossier @backend/api/gantt-application/, puis améliore et optimise le code en respectant les meilleures pratiques de codage. Utilise context7 pour accéder au contexte du projet.
    ```
  </Tab>
  <Tab value="EN">
    ```text title="Prompt EN"
    Explore the code in the files from the @backend/api/gantt-application/ folder, then improve and optimize the code following coding best practices. Use context7 to access the project context.
    ```
  </Tab>
</Tabs>

## Single file

<Tabs items={['FR', 'EN']}>
  <Tab value="FR">
    ```text title="Prompt FR"
    Explore le code du fichier @backend/api/main.ts, puis améliore et optimise le code en respectant les meilleures pratiques de codage. Utilise context7 pour accéder au contexte du projet.
    ```
  </Tab>
  <Tab value="EN">
    ```text title="Prompt EN"
    Explore the code in the file @backend/api/main.ts, then improve and optimize the code following coding best practices. Use context7 to access the project context.
    ```
  </Tab>
</Tabs>

## PRD for optimization

<Tabs items={['FR', 'EN']}>
  <Tab value="FR">
    ```text title="Prompt FR"
    Explore le code du dossier @backend/, puis rédige un prd avec /ralph-tui-prd pour améliorer et optimiser le code en respectant les meilleures pratiques de codage. Utilise context7 pour accéder au contexte du projet.
    ```
  </Tab>
  <Tab value="EN">
    ```text title="Prompt EN"
    Explore the code in the @backend/ folder, then write a PRD with /ralph-tui-prd to improve and optimize the code following coding best practices. Use context7 to access the project context.
    ```
  </Tab>
</Tabs>
