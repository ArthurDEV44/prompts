---
title: Refactor Code
description: Refactor code structure — extract functions, separate responsibilities, apply patterns
---

## Single file

<Tabs items={['FR', 'EN']}>
  <Tab value="FR">
    ```text title="Prompt FR"
    Explore le fichier @backend/api/sources.rs, puis refactore le code : extrais des fonctions, sépare les responsabilités, réduis la duplication, et applique les design patterns appropriés. Utilise context7 pour approfondir ta compréhension de la stack technique, et utilise les skills pertinents de ta bibliothèque si applicables au code refactoré.
    ```
  </Tab>
  <Tab value="EN">
    ```text title="Prompt EN"
    Explore the file @backend/api/sources.rs, then refactor the code: extract functions, separate responsibilities, reduce duplication, and apply appropriate design patterns. Use context7 to deepen your understanding of the technical stack, and use relevant skills from your library if applicable to the code being refactored.
    ```
  </Tab>
</Tabs>

## Folder

<Tabs items={['FR', 'EN']}>
  <Tab value="FR">
    ```text title="Prompt FR"
    Explore le code des fichiers du dossier @backend/api/, puis refactore le code : extrais des fonctions, sépare les responsabilités, réduis la duplication, et applique les design patterns appropriés. Utilise context7 pour approfondir ta compréhension de la stack technique, et utilise les skills pertinents de ta bibliothèque si applicables au code refactoré.
    ```
  </Tab>
  <Tab value="EN">
    ```text title="Prompt EN"
    Explore the code in the files from the @backend/api/ folder, then refactor the code: extract functions, separate responsibilities, reduce duplication, and apply appropriate design patterns. Use context7 to deepen your understanding of the technical stack, and use relevant skills from your library if applicable to the code being refactored.
    ```
  </Tab>
</Tabs>
